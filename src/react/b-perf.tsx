import ReactDOM from 'react-dom/client';
import React from 'react';
import { Table } from './table';

const root = document.getElementById('root') as HTMLElement;
ReactDOM.createRoot(root).render(<Table />);
